This is a production website.
Modify only files required for the current task.
Do not redesign or improve unrelated parts.
Do not touch authentication, pricing, checkout, voting, fan galleries, customer showcase, Supabase, or images unless the task explicitly requires it.
Never delete, move, rename, resize, compress, overwrite, or replace image files.
Prove the root cause before editing.
If the root cause is unknown, stop and report findings.
Prefer the smallest possible change.
Do not commit or push unless explicitly instructed.
Show the exact files changed and why.
Preserve all existing functionality.
Never use broad CSS or fallback JavaScript to hide a deeper problem.
If one component works and another does not, compare them line-by-line before editing.
